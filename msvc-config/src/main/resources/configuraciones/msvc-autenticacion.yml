server:
  port: 8081

spring:
  application:
    name: msvc-autenticacion

  
  datasource:
    url: jdbc:mysql://localhost:3306/msvc-autenticacion
    username: root
    password: juniorzamo1999
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
  
  graphql:
    graphiql:
      enabled: true
    path: /graphql


app:
  jwt:
    secret: secretSuperSeguroDe256BitsParaHMACSHA
    expiration: 86400000
eureka:
  instance:
    hostname: localhost
  client:
    defaultZone: http://localhost:8761/eureka/